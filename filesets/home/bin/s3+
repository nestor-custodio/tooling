#! /bin/env bash
source '/home/nestor/bin/std.lib'

# ---

{ # -- Help Text -- #

	cat <<-EOF | store-as usage
		Usage: $( script-name ) LOCATION [--onto MOUNT_POINT]

		Mounts an S3 LOCATION onto the given MOUNT_POINT.
		If no MOUNT_POINT is given, the default is "~/s3".

		The LOCATION may be a full 'rclone mount'-able "remote:/path" value
		(e.g. 'aws_credentials_remote:/bucket_name/path_to_mount') or an alias
		that translates to one. If no ':' is present in the given LOCATION, ':/'
		will be appended; this allows for easy use of "user + bucket" aliases by
		just the name alone (in which case the bucket *root* is mounted).


		Mandatory arguments to long options are mandatory for short options too.
		  --help                    Show this help text.


		Exit Status:
		  0  if OK,
		  1  if invalid option,
		  2  if failed to mount.
	EOF

}

# ---

{ # -- Parameter Processing: Options -- #

	# Set defaults.

	mount_point="${HOME}/s3"

	# ---
	# ---

	CHAR_OPTS=''
	LONG_OPTS='help,onto:,at:,in:,on:'

	set-params
	while true; do
		option="$1" && shift
		case "$option" in
			(--help) exit-out "$usage" ;;
			(--) break ;;  # No more options.


			(--onto|--at|--in|--on)
				mount_point="$1" && shift
				;;

		esac
	done

}

{ # -- Parameter Processing: Positionals -- #

	location="$1" && shift
	[ -n "$location" ] || error-out 1 'empty LOCATION'


	[ "$#" == 0 ] || error-out 1 'invalid option(s)'

}

# ---
# ---


'has?' ':' <<< "$location" || location="${location}:/"
's3?' "$mount_point" && exit-out 0

{ mkdir -p "$mount_point" && rclone mount "$location" "$mount_point" --daemon --vfs-cache-mode='full' --vfs-fast-fingerprint --s3-directory-markers ;} || error-out 2 'mount failed'
